import { EditorView } from './state';
import { useQuery } from '@tanstack/react-query';
import { TableInitialisationData } from 'common/models/Database';
import { getTableRows } from 'ui/components/tabs/queries';

export function EditorViewTab({ content }: EditorView) {
    return (
        <div>
        </div>
    );
}
